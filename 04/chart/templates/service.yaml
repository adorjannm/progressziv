apiVersion: v1
kind: Service
metadata:
  name: random-site
spec:
  selector:
    app: {{ $.Chart.Name }}-deployment
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
